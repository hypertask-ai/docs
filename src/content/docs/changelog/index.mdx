---
title: Changelog
description: Daily log of new features, bug fixes, and improvements shipped to Hypertask.
---

import { Aside } from '@astrojs/starlight/components';

What's new in Hypertask — updated daily.

<Aside type="tip">
Subscribe to this page's RSS feed or check back daily for the latest updates. Each entry links to the relevant ticket on the Hypertask product board.
</Aside>

---

<Update label="July 17, 2026">
* Improved AI chat defaults — AI chat now opens by default on every task in a side-by-side view, enabling continuous context ([HTPR-4304](https://app.hypertask.ai/detail/project-15/4304))
* Improved Settings navigation — Task scroll options now appear inline without requiring a modal step ([HTPR-4306](https://app.hypertask.ai/detail/project-15/4306))
* Fixed email sign-in outage — Resend plan upgraded and daily quota lifted; magic-link email sign-in restored ([HTPR-4302](https://app.hypertask.ai/detail/project-15/4302))
</Update>

<Update label="July 16, 2026">
* Improved AI usage tracking — hypertask.app page now reflects real gateway spend with timeline filters (7d, today) and data starts July 6 ([HTPR-4312](https://app.hypertask.ai/detail/project-15/4312))
* Improved all AI writing outputs with house style — scannable, BLUF-first, bolded, and non-violent across Improve, Task Writer, AI chat, and generative comments. Write with AI Lengthen requests respect scannability but are not bound by brevity caps. Fix grammar remains minimal. ([HTPR-4313](https://app.hypertask.ai/detail/project-15/4313))
* Fixed task detail header overlapping body on small viewports when AI chat is open — the title row height is unified on tiled half-screen panes. Fixed in two files: TaskDetailComp (updates offset on chat toggle), int-obs (useAutosizeTextArea height calculation on small viewports). Verified live in production. ([HTPR-4321](https://app.hypertask.ai/detail/project-15/4321))
* Added ability to continue previous threads for a specific ticket from the welcome screen — AI chat now shows threads from the question list associated with that task, in addition to earlier-created preview threads ([HTPR-4311](https://app.hypertask.ai/detail/project-15/4311))
* Fixed AI chat drafts replies from the wrong person's perspective — when asked to write the next logical reply, chat now drafts from the user's own perspective for reaction-based messages ([HTPR-4309](https://app.hypertask.ai/detail/project-15/4309))
</Update>

### New Features

- **Settings navigation: team nav shows members, AI usage, billing** — In non-BYOK teams, the Settings Team navigation now displays Members, AI Usage, and Billing sections. API Keys entry is hidden from this section, keeping the UI focused on core team settings. Verified on production (HTPR-4149)
- **AI per-inference metering in first-party data** — Every AI inference call is now tracked in the first-party AiUsage table, with real token counts documented after each deployment. The AI Usage report in Settings now reflects per-inference cost breakdown by feature, model, and user. Verified live in production (HTPR-4131)
- **Auth improvements: Better Auth + BYOK-aware team nav** — Login now uses Better Auth for all email flows with server-led cookies. Non-BYOK teams see team navigation without API Keys entry. Enhanced session handling across sign-ins. Verified deployed (HTPR-4146) — see <a href="https://docs.hypertask.ai/features/collaboration/">Collaboration docs</a> and <a href="https://docs.hypertask.ai/features/ai-features/">AI Features docs</a>. Please review and reply with corrections.

- **Settings and Calendar sidebars use fixed widths at half-screen viewports** — At 1024px width, the Settings sidebar renders at 480px and Calendar boards and filter sidebars at 384px. This provides consistent, predictable layout behavior and prevents truncation of column names and calendar dates. Verified on production across all major screen sizes. ([HTPR-4155](https://app.hypertask.ai/detail/project-15/4155))

- **Direct board archive/delete** — You can archive or delete boards directly from the <strong>Ctrl+K</strong> command palette. Archive removes a board from your active view but preserves it in the Archive section, while Delete permanently removes the board (including all tasks, comments, and customizations). Both actions require confirmation through palette-styled modals (delete requires typing the board name for safety). A first-board redirect takes you directly to newly created boards for immediate use. Verified on production. ([HTPR-4097](https://app.hypertask.ai/detail/project-15/4097))

- **Email authentication fully separated from Firebase** — Email-based sign-in no longer uses Firebase Auth; the new flow returns the full cookie set and redirects directly to the user's board. Magic-link and sign-out remain functional. All authentication flows are now server-led, with Firebase retained exclusively for FCM push notifications. ([HTPR-4172](https://app.hypertask.ai/detail/project-15/4172))

- **Search keyboard navigation** — You can navigate the search results using <kbd>Arrow Down</kbd> and <kbd>Arrow Up</kbd> keys. Pressing <kbd>Arrow Down</kbd> from the search input jumps to the Ask AI row, and <kbd>Arrow Up</kbd> returns focus to the input. The Ask AI row is visually distinguished with a purple accent (`#C668FF`) and bold label. ([HTPR-4121](https://app.hypertask.ai/detail/project-15/4121))

- **Fixed: priority changes in task view now reflect immediately on the board** — When you change a task's priority in the task detail panel, the new value appears on the card immediately instead of waiting for a refresh cycle. Previously, the change took up to 10 seconds to appear on the board. Verified live on production. ([HTPR-4139](https://app.hypertask.ai/detail/project-15/4139))

### Bug Fixes

- **Fixed: comment display when pinned and saved** — Pinning a comment now suppresses the saved/starred indicator, so a pinned+saved comment displays only the orange pinned dot. Previously, both orange and yellow dots appeared simultaneously. Verified live on production. ([HTPR-3876](https://app.hypertask.ai/detail/project-15/3876))

- **Fixed: AI chat HTML sanitization prevents XSS** — All AI chat responses are now sanitized with an isomorphic DOM cleanup library before rendering via `dangerouslySetInnerHTML`. This prevents unsanitized LLM HTML from being injected into the chat interface. Verified deployed in production with PR #1354. ([HTPR-4247](https://app.hypertask.ai/detail/project-15/4247))

- **Post-login toast behavior** — The MCP token toast no longer appears automatically when logging in. The back-end token auto-generation no longer toasts, so you only see the toast when using the explicit Generate button in the MCP modal. Previously, the background auto-generation would trigger an unwanted success message immediately after signing in. ([HTPR-4165](https://app.hypertask.ai/detail/project-15/4165))

- **HTML hydration warning in dev overlay** — The false `<html>` hydration warning from the Next.js dev overlay script has been suppressed. This resolves the developer overlay warning that appeared during development, keeping the dev experience clean. Verified deployed on staging and ready for prod rollout. ([HTPR-4154](https://app.hypertask.ai/detail/project-15/4154))

- **Fixed: email sign-in restored after daily quota exceeded** — Resend plan upgraded; daily quota block lifted. POST /api/auth/send-email-link now returns 200 "Sign-in email sent". Email sign-in is back up. ([HTPR-4302](https://app.hypertask.ai/detail/project-15/4302))

### Infrastructure

- **Auth route cleanup** — Deleted the dead wordpress-signup and password-reset routes. These routes now return 404. The CORS carve-out for the proxy has been removed to simplify the codebase. Firebase Admin is now retained solely for FCM push functionality. ([HTPR-4174](https://app.hypertask.ai/detail/project-15/4174))

---

*(Continue with July 12, 2026...)*