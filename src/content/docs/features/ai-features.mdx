---
title: AI Features
description: AI-powered task creation, voice dictation, text enhancement, and an intelligent co-worker built into your workflow.
sidebar:
  order: 0
---

import { Aside, Tabs, TabItem, Card, CardGrid } from '@astrojs/starlight/components';

Hypertask is built from the ground up as an AI-native project management platform. AI isn't bolted on as an afterthought — it's woven into every part of the workflow, from creating tasks to communicating with your team.

<CardGrid>
  <Card title="AI Task Writer" icon="pencil">
    Describe what you need. Get detailed, context-aware tasks.
  </Card>
  <Card title="Voice Dictation" icon="seti:audio">
    Speak your thoughts. Hypertask transcribes and structures them.
  </Card>
  <Card title="HyperAI Co-worker" icon="comment">
    Chat with an AI that knows your project — and can take action on it.
  </Card>
  <Card title="Text Enhancement" icon="document">
    Improve grammar and readability of any text with one click.
  </Card>
</CardGrid>

## AI Task Writer

Instead of manually writing detailed task descriptions, tell the AI Task Writer what you need in plain language. It generates fully structured tasks with context-aware details.

**How it works:**

1. Open the AI Task Writer from any board.
2. Describe what you need — a sentence or two is enough.
3. The AI uses your **project context** (existing tasks, descriptions, labels) and any **custom instructions** you've configured to generate a detailed task.
4. Review, edit if needed, and add the task to your board.

Web search is handled **automatically** — the AI decides when to look up external information (documentation, APIs, best practices) based on your prompt, without any manual toggle.

The Task Writer maintains a **history of responses**, so you can revisit previous suggestions and reuse them.

<Aside type="tip">
The more context your project has — existing tasks, custom instructions, attached documents — the better the AI Task Writer performs. It learns from your project's patterns.
</Aside>

### Properties during task creation

When creating tasks via the AI Task Writer, you can set the following properties directly without needing a follow-up edit:

| Property | Description |
|----------|-------------|
| Tags | Categorize the task with project labels |
| Task Size | Story points or time estimate |
| Priority | `urgent`, `high`, `medium`, `low` |

### Confirm and discard

When using the AI Task Writer, you can review the task before adding it to your board:

- **Discard changes** — click the discard button or press the discard key to close without saving
- **Add to board** — accept the task and move it to your selected section

Discard and confirm dialogs manage your draft state; they're treated as preview steps before commit.

## Time Tracking

Hypertask includes built-in time tracking for tasks. Tracking is **opt-in per board** by default; time tracking starts automatically the first time you start tracking on a board where it's enabled.

### Enabling and disabling tracking

- **Per board** — Each project board can independently enable or disable time tracking. Shift toggles the state: off by default, enable when you need it, and re-disable when done.
- **Server-side control** — Time tracking state is enforced on the server, so time reporting and history are always consistent across devices and users.
- **Per-task timers** — You can start and stop timers on individual tasks, giving you fine-grained tracking for each activity.
- **Running-timers view** — A dedicated board surface (accessed via shortcut `G-H`) shows all tasks currently being tracked, making it easy to see active work.
- **CLI/AI control** — You can start, stop, and view time tracking through the CLI and AI agents, keeping everything synchronized.
- **Per-inference metering** — Each AI inference call is tracked in the first-party `AiUsage` table, making usage data available in the AI Usage report.

### Time tracking API

When time tracking is enabled on a board, the server enforces tracking for all API interactions:

- **Time API** (`POST /time/start` / `POST /time/stop` / `GET /time`) includes an `enabled: true` flag per board. The API returns `enabled: false` if tracking is disabled on the board.
- **Start tracking** on a board with tracking disabled returns a 403 error.
- **Stop tracking** and paused/unpaused states persist correctly regardless of tracking state.
- **Per-task timers** — Timers can be started and stopped on specific tasks, and the running list surface shows all active timers.
- **Running-timers surface** — The `G-H` shortcut opens a board surface that lists all tasks with running timers, making it easy to review active work.
- **Reports and history** — Use server-managed timer state, not client-side timestamps.

### Integration with workflows

Time tracking integrates with other features:

- **Board level** — Toggle tracking on or off from the board or via selected board shortcut.
- **Task level** — Start and stop tracking from any field in the task detail, or use the CLI/AI.
- **Running list** — View tasks currently being tracked from the board or task detail, or use the `G-H` shortcut to see a dedicated running-timers surface.
- **Per-user and admin defaults** — Display running-timers as a board surface that can be pinned; admins can define default configurations.

<Aside type="tip">
For frequent users, custom ordering lets you prioritize views you use most, while admins can ensure consistent organization for new and rolled-out teams.
</Aside>

## Voice Dictation

Capture ideas hands-free with OpenAI Whisper-powered voice dictation. Use it anywhere you can type — task titles, descriptions, comments.

| Feature | Details |
|---------|---------|
| **Keyboard shortcut** | `Shift + Cmd/Ctrl + D` |
| **Continuous mode** | Keep talking without pressing buttons repeatedly |
| **Echo cancellation** | Works in noisy environments and during calls |
| **Noise suppression** | Filters out background noise for cleaner transcription |
| **Multilingual** | Supports all languages that Whisper recognizes |

Speak naturally. The transcription handles punctuation, formatting, and language detection automatically.

## Text Enhancement

Select any text in a task description or comment and click **Improve Readability**. The AI refines grammar, clarity, and style while preserving your meaning.

- Works on any text field in the app
- Preserves technical terms and project-specific language
- **Undo support** — revert to the original text if you prefer it

This is intentionally lightweight. It's not a rewrite — it's a polish.

## HyperAI Co-worker

HyperAI is an AI chat sidebar built into the Hypertask app. It's context-aware: it knows your project, your tasks, and your team structure.

### What makes it useful

- **Project context** — HyperAI can see your boards, tasks, and project configuration. Ask it questions like "What are the highest priority tasks this week?" or "Summarize what happened on the API project yesterday." It also understands archived tasks, so you can query completed or archived work.
- **Take action** — HyperAI can create tasks, update and delete them, assign or unassign team members, set priorities, manage labels, move tasks between boards, manage sections, update and delete comments, handle inbox notifications, create and delete saved views, and edit sent messages — all directly from the chat sidebar. Just tell it what you need and it handles the rest. Per-inference usage is tracked automatically and reported in the AI Usage breakdown.
- **Smart query analysis** — HyperAI and AI Chat use advanced query analysis to understand your intent and surface more relevant, accurate responses. The AI interprets what you're asking and applies intelligent filtering to match your project context.
- **Consistent HTML output** — Both AI Chat and HyperAI always return responses in HTML, so output renders correctly in the app regardless of the model or prompt used. HTML is sanitized using an isomorphic DOM cleanup library before rendering to prevent XSS attacks.
- **@mentions** — reference specific tasks, people, or projects in your messages to give the AI precise context.
- **Ask AI from comments** — see a comment or description you want to dig into? Click the "Ask AI" button on any comment or task description to send it directly into an AI chat, where you can ask follow-up questions or get analysis.
- **Chat sessions with history** — conversations are saved as named sessions. The AI automatically gives each chat a descriptive name, and you can browse your full chat history to pick up where you left off.
- **Available on mobile** — AI Chat is accessible full-screen on mobile. Tap the chat icon on any mobile view to get the same context-aware assistant you have on desktop.
- **Fast, responsive streaming** — AI chat uses optimized streaming for low-latency responses, with a smooth conversation flow that keeps up with your thinking.
- **Animated thinking indicator** — the moment you send a message, animated thinking dots appear in a new assistant bubble so you always know the AI is processing your request. The dots clear automatically as soon as the first token of the response arrives, giving you immediate visual feedback that your message was received and work is underway.
- **Keyboard navigation in search** — when using the search bar, pressing `<kbd>Arrow Down</kbd>` from the input focuses the Ask AI row, and `<kbd>Arrow Up</kbd>` returns focus to the search input. The Ask AI row appears with a purple accent color (`#C668FF`) to distinguish it from regular search results.

### MCP tools automatically available

AI Chat is powered by the same MCP tools available to external AI agents. When new MCP tools or parameters are added to the Hypertask MCP server, they **automatically become available in AI Chat** — no separate integration step or redeployment is needed. AI Chat always checks for existing MCP tools in the session and makes them accessible to the AI model.

AI Chat now has **full read/write parity with the REST API, MCP, and CLI**. Every action available through those interfaces — including label management, section CRUD, comment updates and deletes, moving tasks between boards, inbox unarchive, task trees, and saved views — is also available from the chat sidebar.

### Attaching files to AI Chat

You can attach files and images directly to messages in AI Chat, giving the AI concrete reference material to work with alongside your prompt.

**Supported content:**

- Images (screenshots, mockups, diagrams, photos)
- Documents (PDFs, text files, spreadsheets, and other common formats)

**How to attach:**

1. Open AI Chat.
2. Click the attachment icon in the message composer, or drag and drop a file into the chat input.
3. Select your file. It appears as a preview alongside your message.
4. Type your prompt and send — the AI receives both the file and your text.

<Aside type="tip">
Attachments are great for asking the AI to analyse a screenshot of a bug, review a spec document, extract action items from a PDF, or get feedback on a design mockup — all without leaving your project context.
</Aside>

### Voice input in AI Chat

AI Chat supports voice dictation, allowing you to speak your messages instead of typing. Use `Shift + Cmd/Ctrl + D` to start recording, or tap the microphone icon in the chat composer.

### Keyboard shortcuts

| Shortcut | Action |
|----------|--------|
| `Cmd/Ctrl + Shift + ?` | Minimize / restore the chat sidebar |
| `Cmd/Ctrl + Shift + O` | Start a new chat |

### What AI Chat can and cannot see

AI Chat's access to your workspace matches what the CLI and MCP tools can read and write — all three interfaces share the same underlying tools and stay in sync.

| Workspace data | AI Chat | CLI / MCP |
|---|---|---|
| Tasks (title, description, status, priority, size) | ✓ | ✓ |
| Assignees & followers | ✓ | ✓ |
| Comments (read, add, update, delete) | ✓ | ✓ |
| Labels (read, create, assign) | ✓ | ✓ |
| Sections / board columns (CRUD) | ✓ | ✓ |
| Subtask hierarchy / task tree | ✓ | ✓ |
| Due dates | ✓ | ✓ |
| Archived / completed tasks | ✓ | ✓ |
| Saved views (create, delete, edit) | ✓ | ✓ |
| Move tasks between boards | ✓ | ✓ |
| Inbox (list, archive, unarchive) | ✓ | ✓ |
| Tags (read, create, assign) by name and ID | ✓ | ✓ |
| Time tracking (start/stop, per-inference metering) | ✓ | ✓ |

## Auto-Summary

When a task accumulates a long comment thread, Hypertask generates an **automatic summary** so you can get up to speed without reading every message.

Auto-summaries highlight:

- Key decisions made in the thread
- Current status and blockers
- Action items that emerged from the discussion

<Aside>
Auto-summaries update as new comments are added, so they always reflect the latest state of the conversation.
</Aside>

## AI Triage

Use the **AI Triage** button in your inbox to automatically categorize, prioritize, and suggest actions for incoming notifications and tasks. The AI analyses your inbox items and provides recommendations for how to handle them efficiently.

## AI Usage & Cost Attribution

Hypertask tracks AI usage across your workspace through the built-in AI Gateway and provides a **cost-attribution breakdown** in **Settings → AI Usage**.

### What's shown

The AI Usage report breaks down usage across three dimensions:

| Dimension | Description |
|-----------|-------------|
| **By Feature** | Usage grouped by AI feature (AI Chat, Task Writer, Text Enhancement, etc.) |
| **By Model** | Usage grouped by AI model (e.g., Claude Sonnet 5, GPT-5.5) |
| **Top Users** | Usage grouped by team member, showing who consumed the most AI resources |
| **By Inference** | Each individual AI inference call is tracked in the first-party `AiUsage` table, including the feature that triggered it, the model used, and the token count |
| **Instant reports** | Per-inference data is available immediately after each deployment, as verified in staging and production |

### Access

The AI Usage report is visible to **project owners and admins**. If the underlying usage data is unavailable, the breakdown section is hidden gracefully.

## Custom AI Instructions

Tailor how AI features behave on a per-project basis.

### Project-level configuration

- **Custom instructions** — write free-text instructions that guide AI behavior. For example: "Always include acceptance criteria in generated tasks" or "Use British English."
- **Attached documents** — upload files that give the AI domain-specific knowledge (RAG). Product specs, API docs, style guides — anything that helps the AI understand your project's context.
- **Model selection** — choose which AI model powers your project's AI features.
- **Visibility** — set instructions as public (visible to all members) or private (visible only to you).

### Supported models

<Tabs>
  <TabItem label="OpenAI">
    - **GPT-5.5** — most capable OpenAI model for complex reasoning and generation
    - **GPT-5.4 mini** — faster and more cost-effective for high-volume tasks
  </TabItem>
  <TabItem label="Anthropic">
    - **Claude Opus 4.8** — strongest reasoning and analysis
    - **Claude Opus 4.7** — powerful reasoning with optimized performance
    - **Claude Sonnet 5** — balanced performance and speed *(default)*
    - **Claude Haiku 4.5** — fastest, ideal for high-volume tasks
  </TabItem>
  <TabItem label="Google">
    - **Gemini 3.5 Flash** — optimized for speed via OpenRouter
    - **Gemini 3.1 Pro Preview** — advanced reasoning via OpenRouter
  </TabItem>
</Tabs>

<Aside type="tip">
Different models have different strengths. Use a more capable model (Opus, GPT-5.5, Gemini 3.1 Pro Preview) for complex task generation and a faster model (Haiku, GPT-5.4 mini, Gemini 3.5 Flash) for quick text improvements and summaries.
</Aside>

## Theme: AMOLED

AMOLED is a specialized dark theme that displays true-black backgrounds for maximum contrast on devices with OLED or AMOLED displays. The theme applies automatic color adjustments to elements that were previously rendered with dark grays.

### Key visual changes

- **True-black base** — backgrounds and backgrounds now render as pure black (0,0,0) instead of dark gray
- **Terminal-style colors** — comment inputs, form fields, and other interactive surfaces use terminal-friendly color palettes optimized for OLED screens
- **Enhanced contrast** — lighter elements pop more clearly against the pure-black background

### Activating AMOLED theme

- **Ctrl+K command palette** — press `<kbd>Ctrl+K</kbd>`, search for **"Theme"**, and select **AMOLED** from the theme list
- **Settings** — navigate to the account section of the sidebar and select AMOLED from the theme picker
- **Available in** — Light, Dark, System, AMOLED, and Paper theme options

### When to use AMOLED

- When using a device with OLED or AMOLED display technology
- For low-light environments where a pure-black display reduces eye strain
- When you want maximum contrast and a distinctive, high-tech aesthetic

<Aside type="tip">
AMOLED is automatically selected by default when your OS is set to "Dark mode" so that badge text remains readable on OLED screens. You can override this via the theme picker if you prefer a dark gray appearance on hybrid-display devices.
</Aside>