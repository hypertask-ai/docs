---
title: Changelog
description: Daily log of new features, bug fixes, and improvements shipped to Hypertask.
---

{/* Starlight has no Update component; local shim so the entries render as
    labeled sections without rewriting them. */}

export const Update = ({ label, children }) => (
  <section>
    <h3>{label}</h3>
    {children}
  </section>
);

<Update label="August 6, 2026">
  * Added: Fast like button appears on task cards and comment rows via hover (task) and inline (comments); tooltip includes [L] hint and renders behind toggle when !hasLiked ([HTPR-3616](https://app.hypertask.ai/detail/project-15/3616))
  * Fixed: Mobile create-task modal no longer overflows the screen; dictation button is present, the action row is bounded, and save/create buttons are fully reachable by swiping on narrow screens ([HTPR-5040](https://app.hypertask.ai/detail/project-15/5040))
  * Fixed: Tasks can now be unassigned even when the agent that created or was assigned to them has been revoked ([HTPR-5049](https://app.hypertask.ai/detail/project-15/5049))
  * Improved: AI chat now understands My Tasks context and can reference your personal workload when responding to questions ([HTPR-5036](https://app.hypertask.ai/detail/project-15/5036))
  * Added: Board sidebar groups by team but sorts teams by most recent board opens so your active teams appear first ([HTPR-5045](https://app.hypertask.ai/detail/project-15/5045))
  * Fixed: Search now returns results on demo boards instead of always showing "No results found" ([HTPR-4989](https://app.hypertask.ai/detail/project-15/4989))
  * Fixed: AI chat writes now respect project boundaries so a task_id is only written to a ticket within the correct project scope ([HTPR-5033](https://app.hypertask.ai/detail/project-15/5033))
</Update>